# Limitations

- large model files are not versioned normally; use Git LFS
- quality depends on the existing lab checkpoints
- only inference is in scope
- marker effects are heuristic and speaker-dependent
